spring:
  servlet:
    multipart:
      max-file-size: 10MB      # 파일 하나 최대 크기
      max-request-size: 10MB
  application:
    name: service
  data:
    redis:
      host: localhost
      port: 6379
  datasource:
    url: jdbc:postgresql://localhost:5435/service_db
    username: appuser
    password: secret_password
  flyway:
    enabled: false
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: service-server
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
server:
  url: http://192.168.0.4:8082  # 이게 있어야 함
  port: 8082
solapi:
  api-key: NCSRQPGIRN51K75S
  api-secret : V76OVL09JQXXNZWXAKXYMFMZHGKMKI96
  from-number : 01090244539

jwt:
  public-key-path: public.pem

file:
  upload-dir: ${user.home}/uploads/images

  logging:
    level:
      org.hibernate.SQL: DEBUG
      org.springframework: DEBUG
      com.zaxxer.hikari: DEBUG